kind = "web"
previewPath = "/"
title = "TrustCheck"
version = "1.0.0"
id = "artifacts/trust-check"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23510

[services.development]
run = "pnpm --filter @workspace/trust-check run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/trust-check", "run", "build" ]
publicDir = "artifacts/trust-check/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23510"
BASE_PATH = "/"
